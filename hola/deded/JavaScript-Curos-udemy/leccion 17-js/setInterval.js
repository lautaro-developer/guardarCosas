/*const tiempo = () => {
    let fecha = new Date()
    let marzo = 'marzo';
    console.log(`${fecha.getHours()}:${fecha.getMinutes()} Mes: ${fecha.getMonth(marzo)} Año: ${fecha.getFullYear()}`)
}

setInterval(tiempo, 1000)
*/

const tiempo = () => {
    console.log('Hola')
}

setInterval(tiempo, 1000)